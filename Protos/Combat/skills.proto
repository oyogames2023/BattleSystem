syntax = "proto3";

package combat;

import "common/entities.proto";

// 定义技能
message Skill {
    int32 id = 1;                   // 技能的唯一标识符
    string name = 2;                // 技能名称
    string description = 3;         // 技能描述
    common.Entity target = 4;       // 技能的目标实体
}
